DFMC_EGL_USE_SURFACEFLINGER = Enable the surface flinger implementation
DFMC_EGL_USE_ANDROID_LINUX  = Enable the native android implementation (bypassing surface flinger)

Platform detection:

_WIN32 = Windows native windows

