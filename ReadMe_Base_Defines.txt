DFMC_DEFINE_NULLPTR          = Force 'nullptr' to be defined as NULL (for old C++ implementations).
DFMC_COMPILER_CPP11_THREADS  = The compiler supports C++ 11 threads (mutex, lock's, etc).
DFMC_COMPILER_TR1_MEMORY     = shared pointers are located in TR1
